
import React, { useEffect, useState } from 'react'; import API from '../services/api'; import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts'; export default function Reports(){ const [daily,setDaily]=useState(null); const [top,setTop]=useState([]); useEffect(()=>{ API.get('/reports/daily').then(r=>setDaily(r.data.data)).catch(()=>{}); API.get('/reports/top-products').then(r=>setTop(r.data.data)).catch(()=>{}); },[]); return (<div><h2 className="text-xl font-bold mb-4">Reports</h2>{daily && <div className="bg-white p-4 rounded shadow mb-4">Revenue: ₹{daily.revenue} | Profit: ₹{daily.profit}</div>}<div className="bg-white p-4 rounded shadow"><ResponsiveContainer width="100%" height={300}><BarChart data={top}><XAxis dataKey="productName" /><YAxis /><Tooltip /><Bar dataKey="soldQty" /></BarChart></ResponsiveContainer></div></div>); }
